<h1>タスク詳細画面</h1>

<table>
  <tr>
    <th>タスク名</th>
    <th>詳細</th>
    <th>ラベル</th>
  </tr>
<br>
  <tr>
    <td><%= @task.title %></td>
    <td><%= @task.content %></td>
    <td>
      <% @task.labels.each do |label| %>
        <%= label.name %>
      <% end %>
    </td>
  </tr>
</table>